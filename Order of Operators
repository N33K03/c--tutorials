#include <iostream>
using namespace std;


    int main() {
        // ()
        // * and /
        // + and -

        double x = (1 + 2) * 3;    //content in the brackets, then multiplication
        cout << x;

    

    return 0;
}
